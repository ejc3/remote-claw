# Secrets & generated artifacts — never commit.
# Raw protocol captures (contain worker_jwt, session data):
captures/
mitm/captures/
# CA + leaf private keys:
mitm/certs/
# Runtime state: client token, relay log, pid:
.state/
__pycache__/
*.pyc
