node_modules/
test-results/
test-results-sqlite/
test-results-turso/
test-results-drain/
test-results-temporal/
test-results-shots/
test-results-zoom/
test-results-evidence/
playwright-report/

# Visual-verification artifacts, all regenerated on demand and compared between commits locally:
#   shots/  — app-e2e.shots.config.ts, 11 surfaces × phone + desktop
#   zoom/   — app-e2e.zoom.config.ts, tight 3× crops + metrics.json (measured geometry)
# The curated before/after PAIRS that back findings in the migration report are NOT here — they live in
# git at docs/assets/astryx/, because docs/astryx-migration.md renders them inline.
shots/
zoom/
shots-baseline/
