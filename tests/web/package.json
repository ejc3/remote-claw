{
  "name": "remote-claw-web-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit",
    "test:app": "playwright test -c app-e2e.config.ts",
    "test:app:temporal": "bash ../../scripts/with-temporal.sh env E2E_BACKEND=temporal pnpm exec playwright test -c app-e2e.temporal.config.ts",
    "test:app:sqlite": "env E2E_BACKEND=sqlite pnpm exec playwright test -c app-e2e.sqlite.config.ts",
    "test:app:drain": "TEMPORAL_NO_WORKER=1 bash ../../scripts/with-temporal.sh pnpm exec playwright test -c app-e2e.drain.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@remote-claw/cli": "workspace:*",
    "@remote-claw/clawsec": "workspace:*",
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
