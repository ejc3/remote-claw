{
  "name": "@remote-claw/clawsec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "remote-claw crypto core — secret format, HKDF key hierarchy, AEAD, canonical AAD, chunking (WebCrypto, runs in Node + browser).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
